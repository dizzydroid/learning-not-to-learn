# Deep learning framework
torch>=2.0
torchvision>=0.15

# Data manipulation
numpy>=1.23
pandas>=1.5 # For manifest creation by user, or if scripts process CSVs

# Visualization
matplotlib>=3.6
seaborn>=0.12

# Progress bar, logging, experiment tracking
tqdm>=4.65
tensorboard>=2.12

# Image processing
Pillow>=10.0

# Testing (optional, for /tests)
pytest>=7.0

# For data fetching
kaggle>=1.5 # For Kaggle API (Dogs vs Cats)
requests>=2.25 # For downloading files from URLs (IMDB-WIKI raw)
gdown>=4.6.0 # For downloading from Google Drive
